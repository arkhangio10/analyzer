services:
  aprendiz:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_ENV: local
      GOOGLE_GENAI_ENABLED: "false"
      GOOGLE_GENAI_USE_VERTEXAI: "false"
    restart: unless-stopped

